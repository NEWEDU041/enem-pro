@AGENTS.md

# ENEM Pro — Project Instructions

Blog post workflow (format, categories, batch commands): see skill `.claude/skills/blog-post-workflow/SKILL.md`.

## Comandos RTK (usar sempre)
```bash
rtk git add .
rtk git commit -m "msg"
rtk git push
rtk next build
```
